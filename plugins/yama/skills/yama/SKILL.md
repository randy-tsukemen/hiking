---
name: yama
description: >-
  日本登山規劃助手。當使用者提到想爬某座日本的山（北阿爾卑斯、八ヶ岳、南阿爾卑斯等）、
  詢問週末/近期適合爬哪座山、想查東京出發的登山巴士（毎日あるぺん号）、
  山小屋住宿、登山路線或山上天氣時使用。
  例如：「我想爬燕岳」「這週末去哪爬山好？」「下週末槍ヶ岳天氣如何？」
  「幫我查去白馬岳的巴士」。透過 yama CLI 取得即時資料後，依使用者需求整理成建議。
---

# yama 登山規劃助手

使用者想規劃日本登山時，用 `yama` CLI 取得即時資料（東京出發的毎日あるぺん号夜行巴士、
山小屋、Yamap 路線圖、山頂 16 天天氣與適宜度），再依使用者的具體需求整理成回答。

## 執行方式

CLI 透過 uvx 直接從 GitHub 執行，**不需要 clone 或安裝**（需要有 [uv](https://docs.astral.sh/uv/)）：

```sh
uvx --from git+https://github.com/randy-tsukemen/hiking yama <指令>
```

首次執行會下載建置（約 10~30 秒），之後有快取。若使用者環境沒有 uv，
請先協助安裝：`curl -LsSf https://astral.sh/uv/install.sh | sh`。

## 指令

```sh
... yama list                    # 收錄的 15 座山（先確認山名有沒有收錄）
... yama <山名> --out <檔案>      # 單座山完整報告（天氣、巴士+預約連結、山屋、路線圖）
... yama <山名> --month 8 --out <檔案>   # 指定巴士查詢月份
... yama <山名> --html <檔案>        # 圖表版 HTML（使用者要分享/存檔時用）
... yama weekend --out <檔案>     # 這週末適合去哪些山（排名+巴士可預約標記）
... yama best --days 14 --out <檔案>     # 未來 N 天最佳登山日排名
... yama hut <山屋名> <YYYY-MM>   # 山屋官網（Yamatan）逐日空位
... yama rooms <course_no> <日期>    # 查套裝方案的山屋「房間」空位（巴士有位≠房間有位）
```

- 山名支援日文漢字、平假名、常見別名與 ヶ/ケ 變體（燕岳、つばくろだけ、木曽駒、涸沢…）。
- 報告很長：**一律用 `--out` 輸出到暫存檔再 Read**，不要直接印在終端機。
- 巴士查詢需要打 API，一份報告約 10〜30 秒。
- `weekend`/`best` 加 `--no-bus` 可跳過巴士查詢（只看天氣，快很多）。

## 回答時的原則

0. 依使用者意圖選模式：
   - **提前規劃**（提到朋友、指定較遠月份、問「有哪些方案」）→ 完整報告
     `yama <山名> --month N`，列出巴士方案與各出發日預約連結——超過 16 天
     沒有天氣資訊沒關係，**先搶位比等天氣重要**（太晚訂就沒位了）。
   - **近期說走就走**（「這週末」「最近哪天好」）→ 由你主導組合，不要直接丟 go：
     先 `get_weather`/天氣表看候選日 → 需要時跟使用者確認偏好（日期彈性？
     預算？要不要住山屋？）→ 對候選日用 verify_trip_candidate 查證事實
     → 依偏好把事實組成建議（使用者說「直接幫我決定」時，用你對他的了解決定）。
     天氣差要不要去、貴一點值不值，是使用者的判斷——把數據給他，別替他決定。
   不確定時先問一句「想先訂之後的日期，還是看近期天氣好的日子？」。
   CLI 只負責取得分散的資料（路線數值、方案、天氣、房間），
   **路線與方案的組合判斷由你依對話脈絡自行完成**，不要套固定模板。
   join 時的原則（不是格式）：路線決定天數與進出點；含住宿的路線優先
   山小屋セット（可實查房間）；異口縱走注意復路上車點。
   **近期/提前皆適用的漸進式流程**（避免一開始就做慢查詢）：
   第一段（快，~5秒）：路線清單（距離/爬升/定数/日程）＋建議住宿（資料庫山屋）
   ＋方案組合（get_bus_options 預設模式：名稱/分類/價格，不含班次）——
   讓使用者先挑「哪條路線＋哪種方案」。
   第二段（使用者選定後才做）：該方案的出發日班次（include_departures=True
   或報告）、房間實查（單日 rooms / 整月 rooms YYYY-MM 掃描）。
   不要未經使用者收斂就掃整月空位——慢且多半浪費。
1. **不要把整份報告貼給使用者**——依對話需求挑重點：
   - 問「什麼時候去好」→ 看天氣表，挑適宜度 ◎/○ 的日子（優先週末），
     並確認該日前一晚有可預約的往路巴士。
   - 問「怎麼去」→ 給 2〜3 個巴士方案（純巴士最便宜的 + 含山屋套裝的），
     附上**該出發日的直達預約連結**（travel-answer 連結，點了直接進預約流程），
     並**同時附上方案詳細頁連結**（`https://bus.maitabi.jp/detail.html?course_no=N`，
     供確認行程說明與乘車地點；注意沒有 /bus/ 前綴）。
   - 問「住哪」→ 優先推薦報告「行程建議」的山屋，附官方預約連結與電話；
     提醒山屋空位要到官網確認，巴士套裝方案可一次搞定巴士+山屋。
2. 出發日旁的 ◎○△× 是登山日（往路夜行巴士＝出發翌日）的山頂天氣適宜度；
   「満席」「受付終了」的班次不要推薦。
- **套裝方案的房間空位**：巴士的「受付中/催行決定」不代表山屋房間有空位。
  推薦含山屋套裝時**必先實查房間**：特定日用 `yama rooms <course_no> <YYYY-MM-DD>`、
  提前規劃推薦整月/多個週末用 `yama rooms <course_no> <YYYY-MM>`（週末掃描）。
  嚴禁只憑巴士的「受付中」就說套裝可訂（曾發生雷鳥荘整月滿房但巴士全顯示受付中）
  （○=有空位、數字=剩餘數、RQ=請求受理、WT=候補、×=已滿）。

- **山屋空位是雙渠道**：巴士套裝（`yama rooms`）與山屋官網（`yama hut <山屋名>
  <YYYY-MM>`，Yamatan 平台）是**分開的庫存**——套裝満房時官網可能有位、
  反之亦然（實例：雷鳥沢 8/8 官網満室但套裝 RQ 可申請）。使用者關心某山屋時
  兩邊都查。監控：`yama watch hut <山屋名> <宿泊日> [--party N]`。
  可查官網空位的山屋（yama hut / watch hut）：Yamatan 系 8 家（涸沢ヒュッテ、
  蝶ヶ岳ヒュッテ、中房温泉、こもれび山荘、仙丈小屋、馬の背ヒュッテ、雷鳥沢ヒュッテ）、
  tenawan 系（雷鳥荘、白馬岳頂上宿舎、太子舘）、燕山荘系（燕山荘、大天荘、
  ヒュッテ大槍）、穂高岳山荘、富士山8家（白雲荘/江戸屋×2/鳥居荘/日の出館/頂上富士館/
  万年雪山荘/池田館）、立山室堂山荘、北穂高小屋、横尾山荘——共 27 家。表單制/電話制山屋誠實告知聯絡方式。
3. 天氣只有未來 16 天有資料；更遠的日期說明無法預測，並建議接近日期再查。
4. 若使用者的山不在 `yama list` 裡，直接說明未收錄，並列出已收錄的山。
5. 適宜度只是規則式參考指標；高風險行程（上級難度、殘雪期）提醒使用者
   出發前再查 てんきとくらす 等專業山岳氣象。
6. 回答的推薦內容用純文字友善格式（使用者可能轉傳）：避免 Markdown 表格與內嵌連結，
   網址直接貼、用「・」條列。
   純文字回覆加**一點點 emoji 當視覺圖示**（轉傳到 LINE 時更容易掃讀）：
   ⛰️山名/標題、📊路線、☀️天氣、🚌巴士、🏠山屋、💰價格、👉預約連結、
   ⚠️注意事項、✅❌⭕空位狀態。原則：當「小節圖示」用（每段開頭一個），
   不要句句都加；狀態符號 ◎○△×、催行決定等日文用語照舊。
7. 使用者用什麼語言問就用什麼語言回答；山名、山屋名保留日文原名。
